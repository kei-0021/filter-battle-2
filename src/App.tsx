
function App() {
  // const [currentTheme, setCurrentTheme] = useState<string>(themes.themes[0]);
  // const [currentFilterKeywords, setCurrentFilterKeywords] = useState<string[]>(foodFilter.keywords);

  return (
    <div className="App">
      <h1>Filter Battle V2</h1>
      {/* <p>現在のお題: {currentTheme}</p> */}
      {/* <p>現在のフィルターキーワード例（抜粋）: {currentFilterKeywords.slice(0, 5).join(", ")}</p> */}
      {/* ここに回答入力コンポーネントやゲーム進行のUIを追加 */}
    </div>
  );
}

export default App;
